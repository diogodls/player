# Desenvolvimento local: copie para .env.local.
# Em Production e Preview, configure a URL HTTPS real da API NestJS no Vercel.
# VITE_* fica publico no bundle: nunca coloque secrets aqui.
VITE_BACKEND_URL=http://localhost:3000
